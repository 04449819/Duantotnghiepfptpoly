import { memo } from "react";

const profilePage = () => {
  return (
    <div style={{ height: "400px", backgroundColor: "red" }}>
      <h1>profilePage</h1>
    </div>
  );
};

export default memo(profilePage);
