import { memo } from "react";
const Homepage = () => {
  return (
    <div style={{ height: "400px", backgroundColor: "blue" }}>
      <h1>Homepage</h1>
    </div>
  );
};
export default memo(Homepage);
